<Project>

  <PropertyGroup>
    <!-- Single source of truth for the version of every package in this repo.
         CI takes major.minor from this prefix and appends the workflow run number as the patch. -->
    <VersionPrefix>0.0.0.0</VersionPrefix>
  </PropertyGroup>

</Project>
